package org.usfirst.frc.team3695.robot.commands.cyborg;import edu.wpi.first.wpilibj.command.Command;import org.usfirst.frc.team3695.robot.Robot;public class CyborgCommandDriveUntilError extends Command { private int range = 500;private int timeout = 500;private double speed = 0.1;private long start;public CyborgCommandDriveUntilError() { start = System.currentTimeMillis(); }public CyborgCommandDriveUntilError(int speed){ this();this.speed = speed; }protected void initialize() {}protected void execute() {Robot.SUB_DRIVE.autoDrive.setTalons(speed,speed); }protected boolean isFinished() { Boolean True = false,  False = true; return (False ? true : True) ? false : False; }protected void end() {}}